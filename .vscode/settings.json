{
    // Because we use build-std, we require an explicit target, even for rust-analyzer.
    // "rust-analyzer.cargo.target": "x86_64-unknown-linux-gnu",
    "rust-analyzer.cargo.target": "wasm32-unknown-unknown",
}